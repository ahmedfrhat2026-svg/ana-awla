name: ana_awla
description: تطبيق شخصي محلي لتتبع المصروفات وتقليل الصرف الاندفاعي
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  sqflite: ^2.3.3
  path: ^1.9.0
  path_provider: ^2.1.4

  flutter_local_notifications: ^17.2.2
  timezone: ^0.9.4

  intl: ^0.19.0
  shared_preferences: ^2.3.2
  share_plus: ^10.0.2
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
